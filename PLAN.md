# HAL名古屋風 LP 制作プラン（実装指示書）

実装担当（Sonnet）への指示書。参考サイトは HAL名古屋トップページ（https://www.hal.ac.jp/nagoya）。
フルページキャプチャを8分割した画像が `reference/slice_00.jpg`〜`slice_07.jpg` にあるので、**実装前に必ず全部 Read して見ること**（上から順にページの上→下に対応）。

## 0. ゴールと前提

- 参考サイトの「レイアウト構造・デザインシステム・世界観」を再現した専門学校風LPを作る。**完全コピーではない**。
- **法的注意（必須遵守）**:
  - 「HAL」の名称・ロゴ・写真・動画・本文テキストは一切使わない
  - 学校名は仮ブランド `NEXA名古屋`（仮称）とし、後で差し替えやすいよう CSS変数/1箇所定義にはできないので、HTML内の表記を統一しておく
  - キャッチコピー・実績数値はすべてオリジナルのダミー（`※数値はダミー` の注記を目立たない形で入れる）
  - 写真・動画はプレースホルダー（CSSグラデーション + SVG）または自前生成画像のみ
- 既存の R&DコンソーシアムLP（ルートの `index.html` / `styles.css` / `script.js`）は**触らない・上書きしない**。
- 新規作成先: `hal-lp/` サブフォルダ。

```
hal-lp/
  index.html
  styles.css
  script.js
  assets/        # 生成SVG・プレースホルダー画像
```

- 技術: 素のHTML/CSS/JS（ビルドなし）。プレビューは `python3 -m http.server 8000` → `/hal-lp/`。

## 1. デザインシステム（参考サイト分析より）

### 1-1. カラー（CSS変数で定義）

```css
:root {
  --c-black: #0b0b0f;        /* ヘッダー・フッター背景 */
  --c-white: #ffffff;
  --c-text: #1a1a1a;
  --c-text-sub: #6b7280;
  --c-blue: #2138d6;         /* ナビ文字・セクションラベル */
  --c-blue-action: #1b6ef2;  /* ピルボタン・リンク */
  --c-ice: #d9e8fa;          /* 背景モザイクタイル */
  --c-ice-light: #eef5fd;
  --grad-main: linear-gradient(100deg, #2b4bf2 0%, #7a3bf5 55%, #ef2fd0 100%);
  /* 数字・見出しの強調はこのグラデを background-clip: text で使う */
}
```

- ページ基調: 白ベース + 薄い水色のモザイク渦背景。テキストは黒、強調はブルー→パープル→マゼンタのグラデ。
- ヘッダーとフッターだけ黒背景・白文字で締める。

### 1-2. タイポグラフィ

- Google Fonts: `Montserrat`（欧文ディスプレイ 700/800）+ `Noto Sans JP`（400/500/700/900）。`font-display: swap`。
- 巨大欧文見出し（例 "ZERO to PRO" 相当）: `clamp(3.5rem, 12vw, 10rem)` / 900 / グラデ文字。
- セクションラベルの型（全セクション共通コンポーネント）:
  - 欧文キャップス（例 `TOPICS`）: ブルー・太字・letter-spacing 0.05em、上に短い横線（幅40px程度のブルーのバー）
  - 直下に日本語サブラベル（例 `トピックス`）を小さく
- 日本語ステートメント見出し: 900ウェイト、`clamp(1.8rem, 5vw, 3.2rem)`、キーワード1語だけグラデ文字にする（参考: 「プロ」「実践力」等の強調パターン）。

### 1-3. 形状・部品

- 大型パネル（動画・コース）: 角丸 24〜32px、影は薄く
- カード: 角丸 12〜16px、白背景、`box-shadow: 0 8px 30px rgba(30,60,150,.08)`
- ボタンは全部ピル型（`border-radius: 999px`）+ 右端に丸囲み矢印アイコン（→）。3種:
  1. 白地・ブルー文字・ブルー細枠（標準「一覧を見る」系）
  2. ブルーベタ・白文字（強め）
  3. グラデベタ・白文字（最重要CTA）
- 矢印アイコンはインラインSVGで共通化

### 1-4. シグネチャ背景「氷の渦」（最重要・このLPの肝）

参考サイトは「薄水色の小さなタイルが渦状に並ぶモザイク + 半透明ガラス質の3Dワイヤーフレーム図形（三角錐・立方体・リング）が浮遊」する背景が全ページに敷かれている。再現方針:

1. `position: fixed; inset: 0; z-index: -1` の背景レイヤーを1つ用意（白ベース）
2. **渦モザイク**: JSで一度だけ生成するSVG（またはassetsに静的SVGとして書き出し）。同心円状に小さな角丸矩形タイルを配置し、中心に近いほど小さく・薄く、外周ほど濃く。レイヤー全体を `animation: rotate 180s linear infinite` で超低速回転。`mask-image: radial-gradient(...)` で中心を白く抜く
3. **浮遊図形**: 三角錐・立方体・リングのアイソメ風ワイヤーフレームSVGを3〜4種作り、8〜12個を絶対配置。各自 `float`（上下±20px）+ `rotate` のkeyframesを duration/delay 違いで適用。`opacity: .5〜.8`、`filter: blur(0〜2px)` で奥行き
4. スクロール連動パララックス: `scrollY * 係数(0.02〜0.08)` を `transform: translateY` に反映（rAFでスロットル）
5. `@media (prefers-reduced-motion: reduce)` で全アニメ停止
6. パフォーマンス: transform/opacityのみ動かす。タイル総数は500個以下に抑える

## 2. ページ構造（上から順・スライス対応）

セマンティックHTML（`header/nav/main/section/footer`）。各セクションはIntersectionObserverでフェードイン。

### ① ヘッダー（slice_00 上部）— `position: sticky`
- 上段: 黒バー。左にロゴ（白の太字テキストロゴ `NEXA名古屋`）、右に小さなユーティリティリンク群「対象者別: 高校3年生 / 高校2年生・1年生 / 大学生・社会人 / 留学生 / 保護者」+「アクセス」「LANGUAGE」
- 下段: 白バー。ブルー太字のグローバルナビ7項目（学科・コース / できること / 就職・資格 / 施設・設備 / キャンパスライフ / 入学要項・学費 / 対象者別）。項目間に細い縦罫線
- **右端固定の縦型CTAレール**（`position: fixed; right: 0; top: 30%`）: 「オープンキャンパス」（グラデベタ・白文字）+「資料請求」（白地・黒文字）の2段。縦書き or 改行2行
- モバイル: 下段ナビをハンバーガー→フルスクリーンドロワーに。CTAレールは画面下部固定バーに変形

### ② ヒーロー（slice_00）
- 全幅より少し内側の巨大角丸パネル（高さ 90vh 程度）
- 中身はプレースホルダー動画風: 暗いグラデーション（#111〜#1a1035）+ ゆっくり動く光のCSSアニメ。`<video>` タグは入れておき poster にグラデ画像、srcは後日差し替え可能とコメント
- 左下に白の巨大見出し「Welcome to NEXA」（2行、Montserrat 800）

### ③ TOPICS（slice_00 下部）
- セクションラベル `TOPICS / トピックス`
- 横並びバナーカード3枚（角丸、影）。ダミー: 「エントリー受付中!」「10月入学 願書受付中!」「全学生にAIツール導入」。各カードはCSSグラデ+大きめタイポで作る（画像不要）

### ④ OPEN CAMPUS（slice_00〜01）
- ラベル `OPEN CAMPUS / オープンキャンパス`
- イベントカード3〜4枚: サムネ（グラデプレースホルダー）+ タイトル + 開催日（例: 7/18(土)・25(土)）。右下に「一覧を見る」ピルボタン
- モバイルは横スクロール（`scroll-snap`）

### ⑤ メッセージ「ZERO to PRO」相当（slice_01）
- 巨大グラデ欧文をパネルに**重ねる**構図: 上に「START to」（ブルー→パープル）、パネル右下に食い込む形で「PRO」（マゼンタ）。`z-index` と負マージンで重ねる
- 中央の黒角丸パネル: ラベル `MESSAGE / メッセージ`、オリジナルコピー（例: 「初心者からプロへ。そのための教育環境がここにある。」）、大きめ白文字の結び「あなたを、プロにする。」、ピルCTA「NEXAだからできること」

### ⑥ 約束ステートメント + PROMISE（slice_01〜02)
- ステートメント: 「NEXAはあなたを **プロ** にすることを約束します。」（「プロ」だけグラデ）
- ラベル `PROMISE / 約束`
- 実績数値ブロック（数字はグラデ文字・カウントアップアニメ、`※数値はダミーです` 注記）:
  - 希望者就職率 **100%**（大きく）
  - コンテスト実績 **No.1**
  - 学生満足度 **97.5%**
- 「3大保証制度」ブロック: 左に「独自 就職・資格・給与の **3大** 保証制度」（「3大」グラデ巨大文字）、右に縦に3つのラベル「完全就職保証」「国家資格 合格保証」「給与保証」+ 「詳細を見る」ボタン

### ⑦ 実践力 + PRACTICE + 企業ロゴ（slice_02〜03）
- ステートメント: 「企業との連携で本物の **「実践力」** が身につきます。」
- ラベル `PRACTICE / 実践教育`。写真プレースホルダー（講義室風グラデ+SVG人影で可）+ 説明文 + ピルCTA2つ
- **ロゴウォール**: 架空企業名10個程度をテキストチップ（白カード・グレー文字・各種フォントweight差）で流し込む。実在企業ロゴは使わない

### ⑧ 数字ステートメント（slice_03）
- 「NEXAだから目指せる **97** の職種。**17** の学科 **9** コースであなたをプロにします。」数字のみグラデ

### ⑨ COURSES（slice_03〜04）— ボリューム最大のセクション
- ラベル `COURSES / 学科・コース`
- ジャンルカード5枚（縦積み・全幅・角丸32px・高さ 420px 程度）: `GAME` / `CG・ANIMATION` / `MUSIC` / `CAR DESIGN` / `AI・WEB・IT`
- 各カード: 背景はジャンル別のダークなグラデ+装飾SVG（ゲーム=HUD風の枠線、CG=セル調グラデ、MUSIC=波形、CAR=メタリック、IT=回路パターン等、CSS/SVGで表現）
- 左に巨大白文字ジャンル名 + 一言タグライン + 丸矢印
- 右に半透明黒パネル: 「4年制」「2年制」の小タグ + コース名のアウトラインピルリンク（各3〜5個、ホバーで白地反転）
- 最後に右寄せ「一覧を見る」

### ⑩ キャンパスライフ + STUDENTS VOICE（slice_05）
- ステートメント: 「同じ夢を持つ仲間と **「好き」** を学ぶ毎日。」
- ラベル `STUDENTS VOICE / 先輩の声`
- カード3枚: カテゴリタグ（GAME等・ブルー小文字）+ 太字の一言 + アバター（SVG円形プレースホルダー）+ 学科・氏名（ダミー名）。**真ん中か右の1枚はブルーベタ・白文字の反転カード**
- 「先輩の声を見る」ピルCTA

### ⑪ 未来ステートメント + GRADUATES VOICE(slice_05〜06)
- 「NEXAで **夢を叶えた未来の自分** をイメージしよう。」（中央部グラデ）
- ラベル `GRADUATES VOICE / 卒業生インタビュー`: メディアカード3枚(サムネ+タグ+見出し+社名/職種+氏名、全部ダミー) + 一覧CTA

### ⑫ CAMPUS（slice_06）
- 中央に縦長のビル画像プレースホルダー（SVGで高層ビルのシルエット/ワイヤーフレームを描く）
- 左に**縦書き**巨大見出し（`writing-mode: vertical-rl`）: 「駅徒歩3分がキャンパス。」
- 右にラベル `CAMPUS / 校舎` + 説明文（駅直結・アクセス良好の趣旨のオリジナル文）+ 「施設・設備を見る」CTA
- 画像下にキャプション小文字

### ⑬ NEWS + POPULAR CONTENTS（slice_06〜07）
- `NEWS / ニュース`: 日付+カテゴリタグ+サムネ+見出しのカード4枚 + 一覧CTA
- `POPULAR CONTENTS / よく見られているページ`: バナーカード4枚（③と同じ作りの使い回し）

### ⑭ 認可・提携バッジ帯（slice_07）
- 薄グレー帯に小さなテキストバッジ3〜4個（「認可校（ダミー）」等）。実在省庁ロゴは使わない

### ⑮ フッター（slice_07）— 黒背景
- ロゴ + SNS丸アイコン5個（インラインSVG: 動画/X/カメラ/f/チャット風の抽象アイコンで可）
- ピルCTA3本横並び: オープンキャンパス / 資料請求 / 問合せ・アクセス
- 巨大電話番号 `052-000-0000` + 受付時間 + 住所（架空）+ MAPリンク
- 姉妹校リンク（NEXA東京 / NEXA大阪 のダミー）
- 4列リンクリスト（対象者別・採用・サイトポリシー等ダミー）+ 検索ボックス（見た目のみ）
- 右上に `TOP` スクロールトップボタン、最下部に `©NEXA`

## 3. JS挙動（script.js）

1. モバイルドロワー開閉（aria-expanded 管理）
2. IntersectionObserver: `.reveal` 要素にフェードアップ（カード群は `transition-delay` でスタガー）
3. 数値カウントアップ（PROMISEの3つ、表示時1回だけ）
4. 背景パララックス（浮遊図形、rAFスロットル）
5. スムーススクロール（アンカー + TOPボタン）
6. 横スクロールカルーセル（OPEN CAMPUS、scroll-snapベースなのでJSは補助のみ）

## 4. レスポンシブ方針

- モバイルファースト。ブレークポイント: 768px / 1024px / 1280px
- コンテナ幅 `max-width: 1200px`、セクション間余白 `clamp(5rem, 12vw, 10rem)`（参考サイトは余白がかなり大きい）
- COURSESカードはモバイルで縦積み内部構成（ジャンル名上・リンク下）
- 巨大タイポは全部 `clamp()` 指定、はみ出し時は `overflow-x: clip` をセクションに

## 5. 実装ステップ（この順で）

1. `reference/slice_00〜07.jpg` を全部見る
2. 雛形 + CSS変数・共通部品（セクションラベル/ピルボタン/カード/reveal）
3. 背景「氷の渦」レイヤー（§1-4）— 先に作って全体の世界観を固定する
4. ヘッダー + 固定CTAレール + モバイルドロワー
5. ヒーロー
6. TOPICS / OPEN CAMPUS
7. MESSAGE（重ね構図）
8. PROMISE（統計 + 3大保証）
9. PRACTICE + ロゴウォール + 数字ステートメント
10. COURSES 5枚
11. VOICE 2種 + ステートメント
12. CAMPUS（縦書き）
13. NEWS / POPULAR / バッジ帯 / フッター
14. JS挙動一式 → レスポンシブ調整 → 最終QA

## 6. QAチェックリスト

- [ ] 375px / 768px / 1280px で崩れ・横スクロールなし
- [ ] 背景アニメが60fpsで動く（DevTools Performance で確認）
- [ ] `prefers-reduced-motion` で全アニメ停止
- [ ] 見出し階層が h1→h2→h3 で正しい、img/svgに alt/aria-label
- [ ] コントラスト: 本文4.5:1以上（薄水色背景上のグレー文字に注意）
- [ ] HAL由来の固有名詞・文言・画像が混入していない（grep で `HAL` を確認。※このファイルとreference/は除外）
- [ ] 全CTAボタンにホバー/フォーカススタイル
- [ ] Lighthouse: Performance / A11y / Best Practices 90+
