# R&D コンソーシアム — リッチデザイン化 実装計画

現行デザイン（藤紫×深紺×明朝のエレガント路線）を土台に、「スタイリッシュでリッチ」な体験へ引き上げるための計画書。**ブランドは変えず、質感と動きを足す**方針。実装時はこのファイルを上から順に消化する。

## 0. 前提・ガードレール（全フェーズ共通）

- ブランド維持: パンフ由来の配色（`--primary: #565eaa` / `--navy: #12172f` / 金 `#a8811f`）と明朝見出しは不変
- 動きは transform / opacity / clip-path のみ（60fps維持）。layout/paintを揺らすアニメ禁止
- `prefers-reduced-motion: reduce` で全演出停止（既存方針を踏襲）
- JS無効でも全コンテンツ閲覧可（`.js` ガード方式を踏襲）
- 各フェーズ完了ごとに Lighthouse: Performance / A11y / Best Practices 90+ を確認
- 静的版（ルート直下）で作り込み → 確定後に WPテーマ（wp-theme/rd-consortium/）へ同期。二重管理期間を最小化するため、フェーズ単位で同期する
- ライブラリ方針: 原則vanilla（IntersectionObserver + rAF）。スクロール連動が複雑化したら GSAP + ScrollTrigger の導入を検討（それ以外のスムーススクロール系・カーソル系ライブラリは酔い/A11y リスクがあるため入れない）

## Phase 1 — 質感の底上げ（クイックウィン、半日〜1日）

見た目の「高級感」を最小コストで一段上げる。

1. **ダークセクションにノイズテクスチャ**: page-hero / statement-band / フッターに極薄のフィルムグレイン（SVG feTurbulence を data URI 化、opacity 3〜4%）。のっぺり感を消す
2. **グラデーションメッシュ**: statement-band の背景に 2〜3 個のぼかした色斑（藤紫/シアン、blur 80px+、超低速 drift アニメ）
3. **文字組の微調整**: 明朝見出しに `font-feature-settings: "palt"` 済 → 加えて主要見出しの手動カーニング（`letter-spacing` の文脈調整）、英ラベルの視覚サイズ統一
4. **カードの質感**: ホバー時に境界線が藤紫→シアンへ流れるグラデーションボーダー（`@property` + conic-gradient 回転）。影を2層化（近距離の締め影+遠距離の柔らかい影）
5. **リンク・ボタンの下線演出**: フッター/ナビのホバー下線を左→右へ描画するアニメに統一
6. **画像の最適化**: hero画像を AVIF/WebP + `<picture>` 化、`fetchpriority="high"`。LCP改善

## Phase 2 — ヒーローの目玉演出（1〜2日）

ファーストビューで「リッチ」を確定させる。

1. **タイトルの分割リビール**: h1「循環型技術創出プラットフォーム」を文字/行単位で `clip-path` マスクリビール（明朝の縦のストロークが立ち上がる演出、stagger 40ms）。tagline→note→CTAの順に時間差で出現
2. **背景の生成パーティクル**: 現行の静止画像の上に、canvas で「光の粒が緩やかに接続される」ネットワーク演出（ノード30個以下・低速・藤紫/シアン）。R&D=つながりのメタファー。tsParticles等は使わず自前実装（~100行）で軽量に
3. **スクロールキュー**: 左下に縦書き「SCROLL」+ 伸縮するライン
4. **ヘッダーの挙動**: スクロールで下降時は隠れ、上昇時に再表示（ヒーローを最大化）。表示時は背景blurを強める
5. 代替案（撮影素材が届いたら）: パーティクルの代わりに**実写ループ動画**（`<video muted loop playsinline>`、poster=静止画、5秒・2MB以下に圧縮）。提案書の「★写真撮影（半日）」の素材を最優先で充てる

## Phase 3 — 「循環」を見せるシグネチャ演出（2日・本計画の核）

「循環型技術創出プラットフォーム」というブランドの核を、スクロールで体感させる。

1. **循環図のSVGライン描画**: ECOSYSTEMセクションに「現場課題 → テーマ設計 → 共同出資 → 研究開発 → 事業化・成果還元 → （現場課題へ戻る）」の円環図を新規作成（インラインSVG）。スクロール進行に応じて `stroke-dashoffset` で円弧が描かれ、各ノードが順に点灯する
2. **sticky ストーリーテリング**: 上記セクションを `position: sticky` 化し、スクロール1.5画面分で円環が一周 → 完成時に中央へ「つくるのは、未来。」がフェードイン。IntersectionObserver + rAF で進行率を算出（GSAP不要の見込み）
3. **FEATURES の関係図強化**: 投資企業⇄エンジニアの菱形図に、双方向へ流れる光の粒（SVG `<circle>` を offset-path で周回）を追加。「価値が行き来している」ことを常時表現
4. **数値の演出余地**: 将来、実績数値（プロジェクト数・参画企業数等）が入り次第、明朝大型数字のカウントアップ+グラデ文字ブロックを PROMISE 的セクションとして追加できる設計にしておく（数値が来るまで実装しない）

## Phase 4 — ページ全体の体験統一（1日）

1. **ページ遷移**: View Transitions API（`@view-transition { navigation: auto; }`）でMPAのままクロスフェード遷移。非対応ブラウザは通常遷移（プログレッシブエンハンスメント）
2. **サブページヒーローの入場演出**: breadcrumb→ラベル→タイトルの時間差リビールをテンプレ化
3. **FAQ の開閉アニメ**: `details` の開閉に `grid-template-rows` トランジション（JSなし）+ Q アイコンの回転
4. **フォームのマイクロUX**: focus時のラベル色変化、入力済みフィールドのチェック表示、送信ボタンのローディング状態（フォーム実装と同時期に）
5. **横スクロールの検討**: 関連会社カード等をモバイルで scroll-snap 横スクロール化（PC はグリッド維持）

## Phase 5 — 素材差し替え・最終品質（撮影後、1日）

1. 撮影素材（社屋・仕事風景・代表インタビュー）で hero / ECOSYSTEM / COMPANY の画像を実写に差し替え。トーンを揃えるため藤紫寄りのカラーグレーディング（CSS filter か事前加工）
2. フォントのサブセット化（使用グリフのみの woff2 を自前ホスト、Google Fonts依存を除去）→ FOUT最小化・表示高速化
3. OGP画像の作成（1200×630、明朝タイポ+藤紫グラデ）、favicon（R&Dモノグラム）
4. 全ページ QA: 375/768/1280/1920px、Lighthouse 90+、reduced-motion、キーボード操作、コントラスト再検査
5. WPテーマへ最終同期 + テーマ側で `wp_enqueue` するJSの分割（ヒーロー演出はトップのみ読み込む）

## 実装順序とマイルストーン

| フェーズ | 内容 | 目安 | 依存 |
|---|---|---|---|
| 1 | 質感の底上げ | 0.5〜1日 | なし（即着手可） |
| 2 | ヒーロー演出 | 1〜2日 | なし（動画版のみ撮影待ち） |
| 3 | 循環シグネチャ演出 | 2日 | なし ※本計画の核 |
| 4 | 遷移・マイクロUX | 1日 | Phase 1〜3 |
| 5 | 実写・最終品質 | 1日 | 写真撮影の実施 |

- 合計目安: 5.5〜7日（撮影待ちを除く）
- 各フェーズ末に before/after スクリーンショット比較 → 承認 → WPテーマ同期、のサイクルで進める
- 着手判断が必要なもの: GSAP導入の要否（Phase 3 で複雑化した場合のみ）、ヒーローを動画にするか生成パーティクルにするか（撮影素材の質次第）

## やらないこと（明示）

- スムーススクロールライブラリ（Lenis等）・カスタムカーソル: 酔い/A11yリスクとブランドの落ち着きに合わないため
- 3Dライブラリ（Three.js）: 演出対効果が低く、モバイル負荷が大きい。パーティクルは2D canvasで十分
- 過度なパララックス多用: 1〜2箇所（背景リング・浮遊キューブ）に限定
